import { View, Text } from 'react-native'
import React from 'react'

export default function Loading() {
  return (
    <View>
      <Text className="text-slate-700">Loading...</Text>
    </View>
  )
}